const { Model, DataTypes } = require('sequelize');
const sequelize = require('../config/connection');
const { User } = require('./User');
const { Subject } = require('./Subject');

class Clue extends Model {}

Clue.init(
    {
        id: {
            type: DataTypes.INTEGER,
            allowNull: false,
            primaryKey: true,
            autoIncrement: true,
        },
        clueText: {
            type: DataTypes.STRING,
            allowNull: false,
        },
        correctResponse: {
            type: DataTypes.STRING,
            allowNull: false
        },
        
        // Took user_id and subject_id out because it is autogenerated by the Clue.belongsTo(User)
        

    },
    {
        sequelize,
        timestamps: true,
        freezeTableName: true,
        underscored: true,
        modelName: 'clue'
    }
)

module.exports = Clue;